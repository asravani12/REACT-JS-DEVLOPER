import React from 'react'
import {createstore,applyMiddleware,compose}from 'redux';
import rootreducer from'./reducer'
import thunk from 'redux-thunk';
const intial State={
    const compose Enchancer=WINDOW-REDUX-DEVTOOLS-EXTENSION-COMOSE-COMPOSE:
    export default create store(rootReducer,initialstate,composeEnhancer(applemiddlewarw(thunk)));
}